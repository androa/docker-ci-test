machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build circleci/app .

test:
  override:
    - docker run --rm circleci/app npm run ci-test

#deployment:
#  hub:
#    branch: master
#    commands:
#      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
#      - docker push circleci/elasticsearch
